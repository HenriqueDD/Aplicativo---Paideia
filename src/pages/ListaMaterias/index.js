import React from 'react'
import {View, Text, Box, ScrollView, VStack, Heading, Center, NativeBaseProvider} from 'native-base'
import {Progress} from 'react-base'

export default function ListaMaterias(){
    <View>
        <ScrollView>
            
        </ScrollView>
    </View>
}